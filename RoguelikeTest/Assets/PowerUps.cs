using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerUps : MonoBehaviour
{

    public GameObject attackUp;
    public GameObject rangedUp;
    public GameObject attackSpeedUp;
    public GameObject rangedSpeedUp;
    // Start is called before the first frame update
    void Start()
    {
        /*GameObject temp = Instantiate(attackUp, new Vector3(1, 5, 0), Quaternion.identity);
        temp.transform.localScale = new Vector3(5, 5, 5);
        temp = Instantiate(rangedUp, new Vector3(-7, 5, 0), Quaternion.identity);
        temp.transform.localScale = new Vector3(5, 5, 5);
        temp = Instantiate(attackSpeedUp, new Vector3(-9, 5, 0), Quaternion.identity);
        temp.transform.localScale = new Vector3(5, 5, 5);
        temp = Instantiate(rangedSpeedUp, new Vector3(-1, 5, 0), Quaternion.identity);
        temp.transform.localScale = new Vector3(5, 5, 5);*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
